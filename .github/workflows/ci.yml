name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Lua
        uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: "5.4"

      - name: Set up LuaRocks
        uses: leafo/gh-actions-luarocks@v4

      - name: Install luacheck
        run: luarocks install luacheck

      # Scope to project dirs: gh-actions-lua installs its toolchain into
      # .lua/ and .luarocks/ inside the workspace, which "." would lint too.
      - name: Luacheck
        run: luacheck lua tests

      - name: Stylua Check
        uses: JohnnyMorganz/stylua-action@v3
        with:
          version: latest
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --check lua tests

  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        # Neovim embeds LuaJIT (5.1-compatible); 5.4 keeps us honest on modern Lua.
        # luajit-2.1.0-beta3 tarballs 404 now; "luajit-2.1" builds the branch head.
        luaVersion: [ "5.4", "luajit-2.1" ]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Lua ${{ matrix.luaVersion }}
        uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: ${{ matrix.luaVersion }}

      - name: Set up LuaRocks
        uses: leafo/gh-actions-luarocks@v4

      - name: Install busted
        run: luarocks install busted

      - name: Run tests
        run: busted -v
